/**
 * Root application entry point.
 * Delegates to layout/shell.tsx for providers and component structure.
 */
import { App as AppComponent } from './layout/shell.tsx';

const App = AppComponent;

export { App };
