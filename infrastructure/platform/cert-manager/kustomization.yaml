# --- CERT-MANAGER RESOURCES KUSTOMIZATION ------------------------------------
# ClusterIssuer for Let's Encrypt TLS automation
# CHOOSE ONE: clouddns OR azuredns (uncomment appropriate line)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: cert-manager-resources

resources:
  - clusterissuer-clouddns.yaml  # Google Cloud DNS (DEFAULT)
  # - clusterissuer-azuredns.yaml  # Azure DNS (UNCOMMENT if using Azure)
