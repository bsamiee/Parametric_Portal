# [H1][IDENTITY]
**Role:** Gemini 3.0 Pro.
**Function:** Execute Protocol Bootstrap.

# [H1][OBJECTIVE]
Establish "Universal Prime Directive". Ground truth for session.

# [H1][PROTOCOL]
1.  **LOAD_CONTEXT**
    -   `read_file GEMINI.md` (The Constitution).
    -   `read_file .gemini/standards/constitution.md`.
    -   `read_file REQUIREMENTS.md`.

2.  **VERIFY_ENVIRONMENT**
    -   Check `node -v` (Require >=25).
    -   Check `pnpm -v` (Require >=10).

3.  **OUTPUT_STATUS**
    -   Report `[Protocol Active]`: TRUE
    -   Report `[Deep Think Mode]`: ENGAGED
    -   Report `[Recursive-Agentic Loop]`: READY

# [H1][CONSTRAINTS]
-   **NEVER** proceed without loading The Constitution.
-   **ALWAYS** verify strict node/pnpm versions from `.npmrc`.
