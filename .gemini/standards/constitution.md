<!-- RATING: 9.8/10 -->
<!-- CRITIQUE: Exceptional rigour. "Immutable Laws" provide perfect determinism. Missing explicit "Rollback Protocol" in laws for failed agentic states. -->
<!-- INTEGRATION: Native enforcement via 'governance-sentinel' extension. -->
# [H1][CONSTITUTIONAL_GUARDRAILS]

## [1][IMMUTABLE_LAWS]
1.  **Pattern Preservation:** **NEVER** introduce alien patterns. Mimic surrounding style (Types -> Schema -> Constants).
2.  **Dispatch Control:** **NO** `if/else` or `switch`. **ALWAYS** use Dispatch Tables.
3.  **Algorithmic Config:** **NO** hardcoded literals. **ALWAYS** derive from single frozen `B` constant.
4.  **Effect Flow:** **NO** `try/catch`. **ALWAYS** use `Effect` pipelines.
5.  **Completeness:** **NO** partial implementations. **ALWAYS** provide complete, working code.
6.  **Type Safety:** **NO** `any`. **ALWAYS** use Branded Types via `@effect/schema`.
7.  **Variable Hygiene:** **NO** `let` or `var`. **ALWAYS** use `const`.
8.  **Iteration:** **NO** `for` or `while`. **ALWAYS** use `.map`, `.filter`, or `Effect` iteration.
9.  **Exports:** **NO** default exports (except `*.config.ts`). **ALWAYS** use named exports.
10. **Wrapper Ban:** **NEVER** create helpers/wrappers serving fewer than 3 call-sites.
11. **Configuration Supremacy:** **NEVER** reimplement logic defined in `biome.json`, `nx.json`, or `vite.config.ts`.
12. **Verification First:** **NEVER** write code without a plan to verify it (Code-as-Action script or Test).
13. **Meta-Silence:** **NEVER** include AI provenance, "Sourced from", or conversational filler in artifacts. Code and docs must appear human-authored.

## [2][VALIDATION_GATES]
-   **Context:** Read full target directory.
-   **Planning:** Critique approach via Swarm.
-   **Routing:** Select Specialist via Router Pattern.
-   **Consistency:** Verify code matches `domain-automation.md` or `domain-components.md`.
-   **Optimization:** Verify minimal LOC solution.
