# [H1][LIST_TOOLS]

List all core, built-in tools available to you. Display in bullet format. Use typescript function syntax with parameters.

Include skill scripts available via `uv run .claude/skills/{skill}/scripts/{script}.py`:
- `nx-tools` — workspace, project, affected, run, tokens, generators, schema, graph, docs
- `exa-tools` — search, code
- `perplexity-tools` — search, deep_research, reason
- `context7-tools` — resolve, docs
- `github-tools` — issues, prs, runs, releases, cache, search
- `tavily-tools` — search, extract, crawl, map