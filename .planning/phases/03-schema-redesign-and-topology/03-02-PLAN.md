---
phase: 03-schema-redesign-and-topology
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/kargadan/harness/src/protocol/dispatch.ts
  - apps/kargadan/harness/src/socket.ts
  - apps/kargadan/harness/src/persistence/checkpoint.ts
  - apps/kargadan/harness/src/config.ts
  - apps/kargadan/harness/src/runtime/agent-loop.ts
autonomous: true
requirements:
  - SCHM-03
  - SCHM-04
  - SCHM-05

must_haves:
  truths:
    - "No file in the codebase contains 'import.*Kargadan.*from.*@parametric-portal/types'"
    - "No file in the codebase references the Kargadan namespace (Kargadan.X) for types or schemas"
    - "dispatch.ts uses locally defined schemas for all type annotations -- zero external schema imports"
    - "socket.ts imports InboundEnvelopeSchema and OutboundEnvelopeSchema from ../protocol/dispatch"
    - "checkpoint.ts uses locally defined persistence schemas for all type annotations"
    - "config.ts accesses CommandEnvelopeSchema.fields.operation and EnvelopeIdentitySchema.fields.protocolVersion from dispatch.ts"
    - "agent-loop.ts imports schema types from ../protocol/dispatch and uses typeof XSchema.Type for all type annotations"
    - "pnpm exec nx run kargadan-harness:typecheck passes with zero errors"
    - "Net LOC across modified files is reduced compared to pre-Phase-3 baseline"
    - "Field names and parameter signatures are consistent between TS harness and C# plugin contracts"
  artifacts:
    - path: "apps/kargadan/harness/src/protocol/dispatch.ts"
      provides: "Protocol dispatch with locally defined schemas and no external Kargadan imports"
    - path: "apps/kargadan/harness/src/socket.ts"
      provides: "Socket handler importing schemas from dispatch.ts"
    - path: "apps/kargadan/harness/src/persistence/checkpoint.ts"
      provides: "Checkpoint service with locally defined persistence schemas"
    - path: "apps/kargadan/harness/src/config.ts"
      provides: "Config using field access from dispatch.ts schemas"
    - path: "apps/kargadan/harness/src/runtime/agent-loop.ts"
      provides: "Agent loop using typeof XSchema.Type from dispatch.ts"
  key_links:
    - from: "apps/kargadan/harness/src/socket.ts"
      to: "apps/kargadan/harness/src/protocol/dispatch.ts"
      via: "import { InboundEnvelopeSchema, OutboundEnvelopeSchema }"
      pattern: "import.*InboundEnvelopeSchema.*from.*dispatch"
    - from: "apps/kargadan/harness/src/config.ts"
      to: "apps/kargadan/harness/src/protocol/dispatch.ts"
      via: "import { CommandEnvelopeSchema, EnvelopeIdentitySchema } + .fields access"
      pattern: "CommandEnvelopeSchema\\.fields\\.operation"
    - from: "apps/kargadan/harness/src/runtime/agent-loop.ts"
      to: "apps/kargadan/harness/src/protocol/dispatch.ts"
      via: "import { CommandEnvelopeSchema, ResultEnvelopeSchema, ... }"
      pattern: "typeof CommandEnvelopeSchema\\.Type"
    - from: "apps/kargadan/harness/src/persistence/checkpoint.ts"
      to: "apps/kargadan/harness/src/protocol/dispatch.ts"
      via: "import { TelemetryContextSchema }"
      pattern: "import.*TelemetryContextSchema.*from.*dispatch"
---

<objective>
Rewire all 5 consumer files to import from colocated schemas (dispatch.ts, checkpoint.ts), replace every Kargadan.* namespace reference with typeof XSchema.Type, and pass the typecheck gate.

Purpose: Complete the schema migration by updating all consumers to use the new colocated schema topology. This is the phase where compilation is restored and the net LOC reduction is verified.

Output: All 5 consumer files updated. Zero Kargadan namespace references remain. Typecheck passes. Net LOC reduced.
</objective>

<execution_context>
@/Users/bardiasamiee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bardiasamiee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-schema-redesign-and-topology/03-RESEARCH.md
@.planning/phases/03-schema-redesign-and-topology/03-01-SUMMARY.md
@apps/kargadan/harness/src/protocol/dispatch.ts
@apps/kargadan/harness/src/socket.ts
@apps/kargadan/harness/src/persistence/checkpoint.ts
@apps/kargadan/harness/src/config.ts
@apps/kargadan/harness/src/runtime/agent-loop.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire all consumer imports and type annotations</name>
  <files>
    apps/kargadan/harness/src/protocol/dispatch.ts
    apps/kargadan/harness/src/socket.ts
    apps/kargadan/harness/src/persistence/checkpoint.ts
    apps/kargadan/harness/src/config.ts
    apps/kargadan/harness/src/runtime/agent-loop.ts
  </files>
  <action>
    Update all 42 Kargadan.* reference sites across 5 consumer files. The research document's "Consumer Dependency Map" and "TS Consumer Types" tables specify every site. Apply these transformations:

    **dispatch.ts (12 sites):**
    - DELETE `import type { Kargadan } from '@parametric-portal/types/kargadan'` (line 5 or wherever it appears)
    - dispatch.ts now owns the schemas locally (from Plan 01). All `Kargadan.X` type references become `typeof XSchema.Type` referencing the local schema consts.
    - Replace `Kargadan.FailureClass` -> `typeof FailureReasonSchema.fields.failureClass.Type` or inline the literal union type
    - Replace `Kargadan.OutboundEnvelope` -> `typeof OutboundEnvelopeSchema.Type`
    - Replace `Kargadan.EnvelopeIdentity` -> `typeof EnvelopeIdentitySchema.Type`
    - Replace `Kargadan.HandshakeEnvelope` -> `typeof HandshakeEnvelopeSchema.Type`
    - Replace `Kargadan.CommandEnvelope` -> `typeof CommandEnvelopeSchema.Type`
    - Replace `Kargadan.HeartbeatEnvelope` -> `typeof HeartbeatEnvelopeSchema.Type`
    - No new imports needed -- schemas are defined in this file.

    **socket.ts (8 sites):**
    - REPLACE `import { Kargadan } from '@parametric-portal/types/kargadan'` with:
      `import { InboundEnvelopeSchema, OutboundEnvelopeSchema } from '../protocol/dispatch'`
    - Replace `Kargadan.InboundEnvelopeSchema` -> `InboundEnvelopeSchema` (schema access)
    - Replace `Kargadan.OutboundEnvelopeSchema` -> `OutboundEnvelopeSchema` (schema access)
    - Replace `Kargadan.InboundEnvelope` -> `typeof InboundEnvelopeSchema.Type`
    - Replace `Kargadan.OutboundEnvelope` -> `typeof OutboundEnvelopeSchema.Type`
    - Replace `Kargadan.EventEnvelope` -> `typeof EventEnvelopeSchema.Type` (import EventEnvelopeSchema from dispatch.ts if needed for this type derivation)

    **checkpoint.ts (6 sites):**
    - REPLACE `import { Kargadan } from '@parametric-portal/types/kargadan'` with: nothing additional -- TelemetryContextSchema already imported in Plan 01, and RunEvent/RunSnapshot/RetrievalArtifact schemas are defined locally.
    - Replace `Kargadan.RetrievalArtifactSchema` -> `RetrievalArtifactSchema` (local)
    - Replace `Kargadan.RunEventSchema` -> `RunEventSchema` (local)
    - Replace `Kargadan.RunSnapshotSchema` -> `RunSnapshotSchema` (local)
    - Replace `Kargadan.RetrievalArtifact` -> `typeof RetrievalArtifactSchema.Type`
    - Replace `Kargadan.RunEvent` -> `typeof RunEventSchema.Type`
    - Replace `Kargadan.RunSnapshot` -> `typeof RunSnapshotSchema.Type`

    **config.ts (2 sites):**
    - REPLACE `import { Kargadan } from '@parametric-portal/types/kargadan'` with:
      `import { CommandEnvelopeSchema, EnvelopeIdentitySchema } from '../protocol/dispatch'`
    - Replace `Kargadan.ProtocolVersionSchema` -> `EnvelopeIdentitySchema.fields.protocolVersion` (field access pattern -- no standalone ProtocolVersionSchema exists)
    - Replace `Kargadan.CommandOperationSchema` -> `CommandEnvelopeSchema.fields.operation` (field access pattern)

    **agent-loop.ts (19 sites):**
    - REPLACE `import { Kargadan } from '@parametric-portal/types/kargadan'` with:
      ```
      import {
          CommandEnvelopeSchema, EnvelopeIdentitySchema, EventEnvelopeSchema,
          FailureReasonSchema, ResultEnvelopeSchema, TelemetryContextSchema,
      } from '../protocol/dispatch'
      ```
    - Replace all `Kargadan.CommandEnvelope` (10 sites) -> `typeof CommandEnvelopeSchema.Type`
    - Replace `Kargadan.EnvelopeIdentity` -> `typeof EnvelopeIdentitySchema.Type`
    - Replace `Kargadan.EventEnvelope` -> `typeof EventEnvelopeSchema.Type`
    - Replace `Kargadan.FailureReason` -> `typeof FailureReasonSchema.Type`
    - Replace `Kargadan.ResultEnvelope` -> `typeof ResultEnvelopeSchema.Type`
    - Replace `Kargadan.TelemetryContext` -> `typeof TelemetryContextSchema.Type`
    - Replace `Kargadan.ProtocolVersion` -> `typeof EnvelopeIdentitySchema.fields.protocolVersion.Type`
    - Replace `Kargadan.CommandOperation` -> `typeof CommandEnvelopeSchema.fields.operation.Type`
    - Replace `Kargadan.EventBatchSummarySchema` -> inline `S.Struct(...)` at the decode site with the 4 fields (totalCount, categories array with inline S.Array(S.Struct({ category, count, subtypes })), containsUndoRedo, batchWindowMs)
    - Replace `Kargadan.RunStatusSchema.Type` -> inline `typeof S.Literal('Created', 'Planning', 'Executing', 'Verifying', 'Persisting', 'Deciding', 'Complete', 'Failed', 'Compensating').Type`

    IMPORTANT: Use `typeof XSchema.Type` consistently (per CLAUDE.md constraint: derive types from schemas). Do NOT create separate `type X = ...` declarations.

    IMPORTANT: Match.tag('value', ...) usages in dispatch.ts and socket.ts that discriminate on `_tag` -- these do NOT need changes since the `_tag` field name is unchanged.

    SCHM-03 is addressed: apps/kargadan consumes packages/ai for services only, no schema imports from packages/ai. SCHM-05 is addressed: consistent field names across TS and C# boundaries (verified in research -- all SmartEnum keys match, CamelCase policy handles casing).
  </action>
  <verify>
    <automated>grep -rn "Kargadan" apps/kargadan/harness/src/ --include="*.ts" | grep -v "node_modules" | head -20; echo "---"; grep -rn "@parametric-portal/types/kargadan" apps/kargadan/harness/src/ --include="*.ts" | head -5; echo "Expected: zero matches for both"</automated>
    <manual>Verify no Kargadan namespace references remain in any of the 5 consumer files.</manual>
  </verify>
  <done>
    All 42 Kargadan.* reference sites across 5 files are replaced with local schema references. No file imports from @parametric-portal/types/kargadan. No Kargadan namespace is used anywhere.
  </done>
</task>

<task type="auto">
  <name>Task 2: Typecheck gate and LOC reduction verification</name>
  <files>
    apps/kargadan/harness/src/protocol/dispatch.ts
    apps/kargadan/harness/src/socket.ts
    apps/kargadan/harness/src/persistence/checkpoint.ts
    apps/kargadan/harness/src/config.ts
    apps/kargadan/harness/src/runtime/agent-loop.ts
  </files>
  <action>
    Run the typecheck gate and fix any compilation errors. The gate is scoped to apps/kargadan only (not full monorepo, per user decision).

    1. Run `pnpm exec nx run kargadan-harness:typecheck`
    2. If errors exist, fix them iteratively. Common expected errors:
       - "Module not found" for `@parametric-portal/types/kargadan` -- means an import was missed in Task 1
       - "Property 'X' does not exist on type 'Y'" -- means a schema field name is wrong
       - "Namespace 'Kargadan' has no exported member" -- means a Kargadan.* reference was missed
       - "Property 'fields' does not exist on type 'Union'" -- means someone tried `.fields` on a Union (use member schema instead)
    3. After typecheck passes, verify net LOC reduction:
       - Count current total LOC across the modified files (use `wc -l`)
       - Compare against the baseline from the research doc (kargadan-schemas.ts was 227 LOC, consumers had ~102 LOC of Kargadan.* references = ~329 LOC schema-related)
       - The projection is ~123-150 LOC net reduction. If positive (more LOC than before), investigate -- net LOC reduction is a hard success criterion per user decision.
    4. Verify no `Kargadan` references exist anywhere in `apps/kargadan/harness/src/`:
       `grep -rn "Kargadan" apps/kargadan/harness/src/ --include="*.ts"` should return zero results
    5. Verify no import of `@parametric-portal/types/kargadan` exists anywhere:
       `grep -rn "@parametric-portal/types/kargadan" . --include="*.ts"` should return zero results

    SCHM-04 verified: each entity has one canonical schema, all derivation via typeof XSchema.Type at call sites.
  </action>
  <verify>
    <automated>pnpm exec nx run kargadan-harness:typecheck 2>&1 | tail -5</automated>
    <manual>Verify zero typecheck errors. Verify net LOC is reduced. Verify zero Kargadan references in the codebase.</manual>
  </verify>
  <done>
    Typecheck passes with zero errors for kargadan-harness. Net LOC is reduced (target: 100+ lines fewer than pre-Phase-3 baseline). No Kargadan namespace references exist anywhere in the codebase. Field names between TS and C# boundaries are consistent.
  </done>
</task>

</tasks>

<verification>
1. `pnpm exec nx run kargadan-harness:typecheck` passes with zero errors
2. `grep -rn "Kargadan" apps/kargadan/harness/src/ --include="*.ts"` returns zero results
3. `grep -rn "@parametric-portal/types/kargadan" . --include="*.ts" | grep -v node_modules` returns zero results
4. Net LOC across all modified files is lower than the pre-Phase-3 baseline
5. dispatch.ts type annotations use `typeof XSchema.Type` exclusively (no separate type declarations)
6. config.ts uses `.fields.operation` and `.fields.protocolVersion` for field access (no literal duplication)
</verification>

<success_criteria>
- Typecheck gate passes: `pnpm exec nx run kargadan-harness:typecheck` exits 0
- Zero Kargadan namespace references in codebase
- Zero @parametric-portal/types/kargadan imports in codebase
- Net LOC reduction of 100+ lines
- All type annotations derive from schemas via typeof XSchema.Type
- config.ts uses field access pattern (CommandEnvelopeSchema.fields.operation) not literal duplication
- C# contracts unchanged -- field alignment maintained through CamelCase naming policy
</success_criteria>

<output>
After completion, create `.planning/phases/03-schema-redesign-and-topology/03-02-SUMMARY.md`
</output>
