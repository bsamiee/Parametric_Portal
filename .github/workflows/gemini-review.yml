name: '🔎 Gemini Review'

on:
  workflow_call:
    inputs:
      additional_context:
        type: 'string'
        description: 'Any additional context from the request'
        required: false

concurrency:
  group: '${{ github.workflow }}-review-${{ github.event_name }}-${{ github.event.pull_request.number || github.event.issue.number }}'
  cancel-in-progress: true

defaults:
  run:
    shell: 'bash'

jobs:
  review:
    runs-on: 'ubuntu-latest'
    timeout-minutes: 15
    permissions:
      contents: 'read'
      issues: 'write'
      pull-requests: 'write'
    steps:
      - name: 'Mint identity token'
        id: 'mint_identity_token'
        if: |-
          ${{ vars.APP_ID }}
        uses: 'actions/create-github-app-token@a8d616148505b5069dccd32f177bb87d7f39123b' # ratchet:actions/create-github-app-token@v2
        with:
          app-id: '${{ vars.APP_ID }}'
          private-key: '${{ secrets.APP_PRIVATE_KEY }}'
          permission-contents: 'read'
          permission-issues: 'write'
          permission-pull-requests: 'write'

      - name: 'Checkout repository'
        uses: 'actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8' # ratchet:actions/checkout@v5

      - name: 'Debug: Environment and Authentication'
        env:
          GITHUB_TOKEN: '${{ steps.mint_identity_token.outputs.token || secrets.GITHUB_TOKEN || github.token }}'
          GH_TOKEN: '${{ steps.mint_identity_token.outputs.token || secrets.GITHUB_TOKEN || github.token }}'
          PULL_REQUEST_NUMBER: '${{ github.event.pull_request.number || github.event.issue.number }}'
          REPOSITORY: '${{ github.repository }}'
        run: |
          echo "=== DEBUG: Environment Variables ==="
          echo "REPOSITORY=${REPOSITORY}"
          echo "PULL_REQUEST_NUMBER=${PULL_REQUEST_NUMBER}"
          echo "GITHUB_TOKEN length: ${#GITHUB_TOKEN}"
          echo "GH_TOKEN length: ${#GH_TOKEN}"
          echo ""
          echo "=== DEBUG: GitHub Event Context ==="
          echo "github.event_name: ${{ github.event_name }}"
          echo "github.event.pull_request.number: ${{ github.event.pull_request.number }}"
          echo "github.event.issue.number: ${{ github.event.issue.number }}"
          echo "github.repository: ${{ github.repository }}"
          echo ""
          echo "=== DEBUG: gh CLI Authentication ==="
          gh auth status || echo "gh auth status failed"
          echo ""
          echo "=== DEBUG: Test PR Access ==="
          gh pr view "${PULL_REQUEST_NUMBER}" --repo "${REPOSITORY}" --json number,title,state || echo "gh pr view failed"
          echo ""
          echo "=== DEBUG: Working Directory ==="
          pwd
          ls -la
          echo ""
          echo "=== DEBUG: .gemini directory ==="
          ls -la .gemini/ 2>/dev/null || echo "No .gemini directory"
          cat .gemini/GEMINI.md 2>/dev/null || echo "No .gemini/GEMINI.md"

      - name: 'Run Gemini pull request review'
        uses: 'google-github-actions/run-gemini-cli@v0.1.18' # ratchet:exclude
        id: 'gemini_pr_review'
        env:
          GITHUB_TOKEN: '${{ steps.mint_identity_token.outputs.token || secrets.GITHUB_TOKEN || github.token }}'
          GH_TOKEN: '${{ steps.mint_identity_token.outputs.token || secrets.GITHUB_TOKEN || github.token }}'
          ISSUE_TITLE: '${{ github.event.pull_request.title || github.event.issue.title }}'
          ISSUE_BODY: '${{ github.event.pull_request.body || github.event.issue.body }}'
          PULL_REQUEST_NUMBER: '${{ github.event.pull_request.number || github.event.issue.number }}'
          REPOSITORY: '${{ github.repository }}'
          ADDITIONAL_CONTEXT: '${{ inputs.additional_context }}'
        with:
          gemini_api_key: '${{ secrets.GEMINI_API_KEY }}'
          gemini_cli_version: '${{ vars.GEMINI_CLI_VERSION }}'
          gemini_debug: true
          gemini_model: '${{ vars.GEMINI_MODEL }}'
          upload_artifacts: '${{ vars.UPLOAD_ARTIFACTS }}'
          workflow_name: 'gemini-review'
          settings: |-
            {
              "model": {
                "maxSessionTurns": 50
              },
              "telemetry": {
                "enabled": true,
                "target": "local",
                "outfile": ".gemini/telemetry.log"
              }
            }
          prompt: '/gemini-review'
